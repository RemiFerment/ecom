<h1>Gestion des produits</h1>
<div class="product-list">
    <a href="add_product.php" class="btn-add">Ajouter un produit</a>
</div>
<div class="product-grid">
    <?php foreach ($products as $product): ?>
        <div class="product-card">
            <div class="product-id">ID: <?= $product['id']; ?></div>
            <h3><?= $product['title']; ?></h3>
            <p class="product-description"><?= $product['description']; ?></p>
            <div class="product-price"><?= $product['price']; ?> €</div>
            <div class="product-actions">
                <a href="edit_product.php?id=<?= $product['id']; ?>" class="btn btn-edit">Modifier</a>
                <a href="delete_product.php?id=<?= $product['id']; ?>" class="btn btn-delete" onclick="return confirm('Êtes-vous sûr de vouloir supprimer ce produit ?');">Supprimer</a>
            </div>
        </div>
    <?php endforeach; ?>
</div>